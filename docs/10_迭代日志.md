# 「星图」迭代日志（操作流水账 · 实时更新）

> **本文件是项目唯一操作流水账：任何代码/数据/文档改动，必须在当天补记一行。**
> 规则（铁律）：
> 1. 每次改动前先读本日志，了解最近在做什么；
> 2. 改动完成后**当天**在本日志追加一条记录：日期、类型、做了什么、改了哪些文件、是否需要同步其他文档（docs/00~09）；
> 3. 如果改了功能/数据模型/提示词，同步更新 `00_总文档_v1.0.md` 与相关文档，并在日志中注明"已同步"或"待同步"；
> 4. 日志只追加不修改历史记录（写错了就新增一条"更正"记录）；
> 5. 每次 git 提交信息应能对应到本日志的一条记录。

---

## 模板（复制到最新日期下方）

```
- [日期] [类型: 功能/修复/数据/文档/部署/发布] 一句话说明。
  - 文件：改动/新增的文件列表
  - 文档同步：已同步 xxx / 待同步 xxx / 不涉及
  - Git：commit hash（有则填）
```

---

## 2026-08-23

- [2026-08-23] [修复] P0 质量欠账三件：周期报告空数据最小化 + cohort 禁令与统一人称、Key 泄漏清理、docs/04 报告结构同步（五段式 → 叙事优先）。
  - 文件：`star-map/lib/engine.js`（空数据守卫 + `PERIOD_CACHE_VERSION=2`）、`star-map/lib/prompts.js`（周期报告提示词三条硬约束、移除 starSign）、`star-map/app/api/report/route.js`（缓存版本校验）、`star-map/.env.example`（占位符化）、`docs/04`、`docs/02`、`docs/README.md`、`docs/09`（新增评审文档+修复记录）
  - 文档同步：已同步（docs/09 第七节有详细修复记录）
  - Git：00b5e1b、ca5eeee
- [2026-08-23] [部署] 沙箱环境无法常驻 dev server（fork 管道限制 + 后台任务回收），改用**生产模式**：`npm run build` + 独立进程 `next start`（PID 14496，端口 3000）。以后代码改动需重新 build + 重启。
  - 文件：`.next/`（构建产物，不入库）
  - 文档同步：不涉及（docs/00 §14 运行方式待补生产模式说明）
  - Git：无
- [2026-08-23] [发布] 推送到 GitHub：`git@github.com:roxanna223/xingtu.git`（私有仓库，默认分支 main）。本机新增专用 SSH Key `~/.ssh/id_ed25519_xingtu`（仓库级 `core.sshCommand` 配置，不影响其他账号）。
  - 文件：git 配置（.git/config）
  - 文档同步：不涉及
  - Git：本地 main = ca5eeee，远端 main = ca5eeee
- [2026-08-23] [文档] 建立本迭代日志（docs/10），并约定"每次改动必须当天记日志"。
  - 文件：`docs/10_迭代日志.md`（新增）、`docs/README.md`（索引+规则说明）、`docs/00_总文档_v1.0.md`（文档索引表）
  - 文档同步：已同步
  - Git：待提交
- [2026-08-23] [数据] 制作 20 个月连贯测试数据集 `data/测试数据-20个月日记.md`（2025-01 ~ 2026-08，约 110 天，覆盖日/周/月/季/年报 + 全 8 情绪 + 倾诉/求助意图），导入画像用于产品体验测试。导入后清空报告缓存（lastReport/reports/periodReports），各周期报告按需重新生成。
  - 文件：`data/测试数据-20个月日记.md`（新增）、`star-map/data/profile.json`、`star-map/data/days.json`（运行时数据，不入库）
  - 文档同步：不涉及（纯测试数据）
  - Git：待提交（数据文件入库，运行时数据不入库）

---

## 待办 / 下一步（日志内跟踪）

- [ ] 体验验证：日报/周报/月报/季报/年报生成 + 星图演化 + 小星对话感受（等待导入完成后逐项测试）
- [ ] P1：对话原文持久化并入画像、反馈消费闭环（adaptLog 落盘）、主页数值真实化
- [ ] DeepSeek Key 轮换（控制台作废旧 Key → 更新 `star-map/.env`）
- [ ] docs/00 §14 补充生产模式运行说明
